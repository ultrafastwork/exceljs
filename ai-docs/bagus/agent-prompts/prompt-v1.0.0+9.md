# AI Agent Prompt: Implement PR #2962 Handle Missing r Attribute in Row and Cell Elements

## Objective
Your goal is to implement PR #2962: "fix: handle missing r attribute in row and cell elements (#2961)" by introducing an address tracking system in `RowXform` to generate sequential `r` coordinates when they are missing.

## Context & Details
- **PR Description**: When parsing Excel files generated by database export tools (like DataGrip/IDEA), they may lack the `r` attribute on `<row>` and `<c>` cell elements. ExcelJS currently throws "Invalid row number in model" because it expects explicit references.
- **Rules File**: Always adhere to the project rules in [.windsurfrules](file:///d:/projects/exceljs/.windsurfrules).

## Instructions

### 1. Implement Address Tracking in `RowXform`
- Modify `lib/xlsx/xform/sheet/row-xform.js` to track sequential coordinates when parsing `<row>` and cell elements without `r` attributes.
- Ensure the column counter is reset at the start of each row.

### 2. Add New Integration Tests
- Add a new integration test suite under `spec/integration/issues/issue-2961-missing-r-attribute.spec.js` using a DataGrip-generated Excel file.
- Verify the parser successfully reads and positions cells (A1, B1, C1, etc.) correctly without throwing errors.

### 3. Verification
- Verify your changes using existing unit and integration tests (`pnpm test:unit` and `pnpm test:integration`).
- Run lint checks (`pnpm lint` and `npx eslint`) to ensure styling compliance.

### 4. Documentation & Handoff
- Follow the **Handoff (End of Session)** rules in [.windsurfrules](file:///d:/projects/exceljs/.windsurfrules) to update the handoff file.
